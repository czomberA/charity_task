package org.example.sii_task.repositories;

import org.example.sii_task.models.collected.Collected;
import org.springframework.data.repository.CrudRepository;

public interface CollectedRepository extends CrudRepository<Collected, Long> {
}
